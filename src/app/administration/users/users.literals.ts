import { Literal } from 'src/assets/i18n/literals';

export const UserLiteral = {
  title: Literal.administration.users.title,
  username: Literal.administration.users.username,
  name: Literal.administration.users.name,
  surname: Literal.administration.users.surname,
};
